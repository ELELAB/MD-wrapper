# MD-wrapper
Pipeline for MD simulation

the wrapper should be run as
> nohup python MD_local_FINAL.py directory_name pdb-folder_name no_of_cores force_field water_model -v >& log &

with "Files" and a folder containing pdb files in the same directory.
